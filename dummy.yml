name: tf-bare
version: 0.0.1
description: a demo tensorflow model
labels: ["tensorflow", "bare"]
image: dummyai/python3  
assets:
    - results/checkpoint
setup:  
    - pip install -r requirements.txt
